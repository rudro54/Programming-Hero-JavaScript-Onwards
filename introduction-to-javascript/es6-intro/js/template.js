const multiline = ` first lien
second line 
third line
forth line`;

// console.log(multiline);


const a = 30;
const b = 20;

const nums = [78, 98, 45, 12, 6, 444];

const summary = `sum of ${a} and ${nums.length} is : ${a + b} `;
console.log(summary);