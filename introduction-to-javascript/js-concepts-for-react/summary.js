/* 
Fundamentals :
1.1 variable (let, const)three types of variable
1.2 condition (<, >, ===, !==, <= ,>=,)&& ||
1.3 array declare , length , index , push , pop
1.4 for loop , while 
1.5 function return parameter
1.6 object property , including array , object

------------------------------------------------ 
ES6
1. template string ${}
2. spread(...)
2.1 copy and array and add a new element to an array
2.2 use filter to remove an item from an array
3. arrow function
3.1 no parameter
3.2 single parameter 
3.3 multiple parameters
3.4 multi line
4. destructuring : object destructuring array destructuring
5. object shorthand
6. function paramter default value
7. optional chaining (? .)
----------------------------
Browser API
1. Local Storage & Session Storage
2. location api
3. history api
4. fetch
-----------------------------
others 
1. array map, foreach, filter, find
2. ternary operator
3. logical and  logical or && || 

*/

const x = 50;
const y = 100;

const obj = {
    x: x,
    y: y

}

// shorthand 
const obj1 = { x, y };

const name = 'solimullah';
const area = 'Dhaka';

const nobab = {
    name: name,
    area: area
}

// shorthand
const nobab2 = { name, area };